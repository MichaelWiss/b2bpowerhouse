// Phase 3 — Predictive reorder forecast cron (placeholder)

import { NextResponse } from "next/server";

export async function GET() {
  return NextResponse.json(
    { message: "Reorder forecast cron — coming in Phase 3" },
    { status: 501 },
  );
}
