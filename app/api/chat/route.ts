// Phase 3 — AI reorder assistant endpoint (placeholder)
// Will use Vercel AI SDK with Groq or Ollama

import { NextResponse } from "next/server";

export async function POST() {
  return NextResponse.json(
    { message: "AI reorder assistant — coming in Phase 3" },
    { status: 501 },
  );
}
